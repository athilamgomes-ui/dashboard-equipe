# RUNBOOK — Dashboard de Vendas (dashboard_amgomes)

> Extraído do CLAUDE.md global em 04/07/2026 (reestruturação em 3 camadas).
> **Carregue este arquivo SEMPRE que for trabalhar no dashboard de vendas** (pipeline, coleta, YoY, KPIs, HTML).

## Grupo A.M. Gomes — Dashboard de Vendas (dashboard_amgomes)

⚠️ **REGRA CRÍTICA — NUNCA usar Chrome MCP para coletar dados do Microvix neste dashboard. Usar SEMPRE Playwright headless.**

**Causa raiz (aprendido em definitivo 30/05/2026):** as skills `dashboard-amgomes-update` (12:06) e `dashboard-amgomes-update-tarde` (18:30) usavam Chrome MCP para abrir o Microvix. Em execução agendada o Chrome fica em background → a extensão Claude in Chrome **perde a conexão WebSocket** e TODA coleta falha silenciosamente. Sintomas nos logs: `"Claude in Chrome is not connected"`, `navigate` travando 300s, `ConnectionRefused`, `socket connection closed`. O dashboard ficava com timestamp "Atualizado" antigo (da última atualização manual) enganando o usuário. Isso quebrou em 28/05, 29/05 e 30/05 seguidos e abalou a confiança nos dados. **Antes (28/05) o bug era duplo:** os SKILL.md também tinham caminho errado (`/Desktop/claude/dashboard_amgomes.html` sem `dashboard-equipe/`) + comando `cp` que sobrescrevia o arquivo canônico do repo com cópia estática a cada execução. Ambos corrigidos.

⚙️ **PIPELINE DETERMINÍSTICO (implementado 01/06/2026) — esta é a forma canônica de rodar o dashboard.** Um único comando, SEM LLM no caminho: `bash /Users/elkgomes/Desktop/claude/dashboard-equipe/atualizar_amgomes.sh` (~40s). Faz: coleta (Playwright) → YoY light/full (`atualiza_fatmensal.mjs`) → **render determinístico** (`scripts/build_amgomes.mjs`) → sanity-check → commit/push. Tem **lock** (`/tmp/amgomes_update.lock`) que impede execuções concorrentes (a corrida de escrita que quebrou o dashboard em 01/06). Exit codes: 0=ok, 10=coleta falhou (PRESERVA versão anterior, não publica lixo), 20=build falhou (restaura HTML), 30=lock. As duas tarefas agendadas (12:06 e 18:30) agora são **wrappers finos** que só chamam esse script e reportam — o agente NUNCA edita o HTML. **`build_amgomes.mjs` é o ÚNICO escritor do HTML**: regenera KPIs (ranking por %meta do período via pesos de dia útil, badges), arrays faturado/maiAcum/metas, rótulos+ordem dos gráficos, e o bloco `vendedores` (preservando `m12` por nome via match do primeiro nome). Config no topo do script: `META_MENSAL`, `LOJA`, `PESO_DIA`. Razão da mudança: TODOS os bugs (caminho errado, `cp`, timestamp mentiroso, KPI cards pela metade, corrida) vinham de um agente LLM editando HTML "na mão" — o pipeline determinístico elimina isso (mesma arquitetura do dashboard de Compras). Para rodar manual: o comando acima. Modelo de coleta abaixo (os scripts continuam os mesmos):

⚠️ **EXCLUIR CLIENTE 8 (R MAURA DE FREITAS) DO TOTAL DE VENDAS (implementado 30/06/2026, pedido "se trata de venda de uma loja nossa para outra loja nossa").** O cliente cód **8** é uma das próprias lojas comprando da outra — venda **entre lojas**, não pode contar como venda real. Hoje aparece só na **L5** (R$ 11.887,58 / 667 pç no mês corrente), mas o filtro roda nas 4 lojas pra ser robusto. **Subtração na FONTE (decisão de arquitetura):** os DOIS coletores de faturamento subtraem o cliente 8 antes de emitir JSON, então TUDO a jusante (build KPIs/ranking/maiAcum, fatMensal light/full, array `faturado`) fica consistente sem precisar excluir em vários lugares:
   - `coleta_amgomes_lojas.mjs` (acumulado/KPIs): após montar `out`, para cada empresa [1,3,4,10] roda o Ranking de Clientes do período e subtrai de `cells[5]` (V.Líquida) e `cells[1]` (Qtde). ⚠️ Reescreve essas células como **inteiros** (não BR "11.887,58") porque `intBR` trata `.` como milhar — inteiro parseia certo; perde só os centavos no display (KPI já usa fmtMil sem centavos). `cells[8]` (margem%) fica intocado.
   - `coleta_amgomes_mensal.mjs` (YoY): por mês × loja subtrai o ranking do cliente 8 do `porEmp` antes de empilhar. Corrige o card mês a mês E o `faturado` (mês fechado, lido do fatMensal).
   - Helper compartilhado **`cliente8_ranking.mjs`** (`rankingCliente(page,emp,di,df,cod="8")`) = mesma trilha do `coleta_cliente8.mjs` da premiação (Faturamento→Ranking de Clientes, `relatorio_ranking.asp`). Ambas as subtrações são **não-fatais** (ranking que falhar numa loja/mês subtrai 0 e loga — nunca derruba o pipeline; pior caso mostra valor cheio, não quebra). Const `COD_EXCLUIR="8"` no topo dos dois coletores.
   - **Reprocessar histórico do YoY sob demanda:** `FORCE_FULL=1 bash atualizar_amgomes.sh` força o caminho full (recoleta os 2 anos mês a mês já líquidos do cliente 8) mesmo fora de segunda/virada de mês. Usado na implantação 30/06 pra limpar o histórico de uma vez.
   - Mesma regra já existe na **premiação** ([[premiacao_excluir_cliente8_l5]]) via `coleta_cliente8.mjs`+`coleta_premiacao_ci.mjs`. Se um dia o cliente 8 passar a comprar legitimamente (improvável), reavaliar.

**Fluxo correto (já gravado nos dois SKILL.md):**
1. **Coleta SEMPRE via Playwright headless** — dois scripts em `/Users/elkgomes/Desktop/claude/dashboard-equipe/scripts/`:
   - `coleta_amgomes_lojas.mjs "DI" "DF"` → faturamento por loja. Saída JSON: `{"1":{nome,cells[...]},"3":...,"4":...,"10":...}`. V.Líquida=cells[5], Qtde=cells[1], Margem%=cells[8]. ~21s. **Já subtrai o cliente 8 (venda entre lojas) na fonte — ver regra acima.**
   - `coleta_amgomes_vendedores.mjs "DI" "DF"` → vendedores via API REST. Saída: `{L1:[{n,v,t,tm,pecas}],L3,L4,L5}`. Já filtra VENDEDOR PADRÃO.
   - Rodar os dois em **paralelo** (background `&` + `wait`). DI=`01/MM/AAAA`, DF=hoje (`date +%d/%m/%Y`).
   - Autenticação: `microvix_auth.mjs` lê Keychain (account=`microvix-cron`, services=`amgomes-microvix`/`amgomes-microvix-user`) + perfil persistente `~/.claude/microvix-profile`. Não depende de Chrome aberto.
2. **Arquivo canônico ÚNICO:** `/Users/elkgomes/Desktop/claude/dashboard-equipe/dashboard_amgomes.html` (repo `dashboard-equipe`, GitHub Pages). Editar SEMPRE direto nele — **nunca** usar `cp` de outro caminho. (Existe um `/Desktop/claude/dashboard_amgomes.html` órfão/estático de 28/05 que deve ser IGNORADO — não é fonte da verdade.)
3. **Publicar:** `cd /Users/elkgomes/Desktop/claude/dashboard-equipe && git add dashboard_amgomes.html && git commit && git push origin main`. URL: https://athilamgomes-ui.github.io/dashboard-equipe/dashboard_amgomes.html
4. **Fallback** se Playwright falhar: ler `/Users/elkgomes/.claude/projects/-Users-elkgomes-Desktop-claude/memory/lojas_dados_erp.md` e **avisar o usuário explicitamente que usou fallback** (nunca deixar o dashboard parecer atualizado com dado velho).

⚠️ **BUG DO TIMESTAMP MENTIROSO (corrigido em definitivo 01/06/2026) — NUNCA reintroduzir.** Havia uma função `updateTime()` que fazia `document.getElementById('lastUpdate').textContent = 'Coletado às ' + new Date()...` a cada abertura da página. Resultado: o dashboard SEMPRE mostrava a hora em que o usuário ABRIU a página (ex.: "21:26") como se fosse a hora da coleta — mentindo sobre dados velhos e destruindo a confiança. **Regra:** `#lastUpdate` é texto ESTÁTICO escrito pela skill (hora real da coleta: "Atualizado em DD/MM/AAAA às HH:MM"). O JS só pode ESPELHAR esse valor no rodapé (`#genDate`), NUNCA sobrescrever com `new Date()`. Qualquer timestamp exibido tem que ser o da coleta real, não o de page-load.

⚠️ **CORRIDA DE ESCRITAS CONCORRENTES (causa raiz 01/06/2026).** Rodar DUAS sessões/agentes Claude ao mesmo tempo mexendo no mesmo `dashboard_amgomes.html` (ex.: uma execução agendada + uma sessão manual, ou duas janelas do Claude Desktop) faz cada uma fazer read-modify-write e o arquivo vira uma MISTURA inconsistente (ex.: dados JS de junho + KPI cards de maio + nada commitado). Sintoma: dashboard "pela metade" e working tree sujo sem push. **Regra:** NÃO trabalhar no dashboard com outra sessão Claude ativa mexendo nele; se for editar manualmente, garantir que nenhuma execução agendada esteja rodando. Ao corrigir, gravar UMA versão completa e consistente de uma vez e commitar/pushar IMEDIATAMENTE (git é a fonte da verdade; o GitHub Pages serve o último push). As duas tarefas agendadas (12:06 e 18:30) rodam em horários distintos e não colidem entre si — o risco é sessão manual concorrente.

**Edições no HTML (mapa rápido):** `maiAcum` ordem [L5,L4,L1,L3]; 4 KPI cards reordenados por %meta (rank1=#ffb347...rank4=#ef4444); bloco `vendedores` entre `VENDEDORES_INICIO/FIM` (preservar campo `m12`); badge data + `id="lastUpdate"` + título Seção 1. **4 lojas:** L1=emp1 Casa Altamira, L3=emp3 Casa Itaituba, L4=emp4 MissBeleza Altamira, L5=emp10 MissBeleza Santarém. Metas Mai/2026: L1=140k L3=80k L4=140k L5=90k.

**Quadros alinhados ao ranking (25/06/2026):** os 3 grids por loja — KPIs, Vendedores e Gráficos — agora seguem a MESMA ordem do ranking (cada loja corresponde posição a posição). O `build_amgomes.mjs` regenera os grids de Vendedores e Gráficos via marcadores `VENDGRID_INICIO/FIM` e `CHARTSGRID_INICIO/FIM` na ordem do `ranking` (config tem `vendTitle`/`chartTitle` por loja; label "Méd 12M" calculado dinâmico). Não editar esses grids à mão.

**Quadro "Top 10 Marcas por Loja" (Seção 2, criado 25/06/2026):** grid de 4 lojas (ordem do ranking), cada uma com seu top 10 marcas por UNIDADES vendidas no ANO (01/01→hoje). Fonte: `coleta_top_marcas.mjs` (novo, Playwright headless) = relatório de saldo do ERP (`relatorio_compra_venda_saldo_empresa.asp`, Analítica agrupado por Marca, **exibe_estoque_transito=true → tabela de 11 colunas, vendas=c[8]**, janela data1=01/01/AAAA) por empresa; saída `{geradoEm,periodo,L1:{Marca:un},L3,L4,L5}` → `/tmp/topmarcas_out.json`. ⚠️ **Regra de negócio:** na Santa Clara, EXCLUIR produtos "LIXA" (constante `EXCLUIR_PRODUTOS={'santa clara':/\bLIXA\b/i}`) — são dados como troco e infl­avam o ranking (eram 65% da marca). Roda no `atualizar_amgomes.sh` (passo 2.7, retry 2x, preserva se falhar). `build_amgomes.mjs` (seção 7) gera o grid entre marcadores `TOPMARCAS_INICIO/FIM`, marcas em title-case, período em `#topMarcasPeriodo`. Para excluir mais categorias genéricas (ex.: Impala, Importados) é só ampliar a regra de exclusão.

**Linha "Vendedor Padrão" na aba VENDEDORES (04/07/2026, pedido do usuário).** A linha "Total" de cada loja mostra `totalV = lojaTotais[loja]` (V.Líquida real do ERP, = `maiAcum`), mas as linhas de vendedores nomeados somam menos — o `VENDEDOR PADRÃO` (vendas sem vendedor atribuído) é filtrado no coletor. Esse gap aparecia como diferença silenciosa entre a soma dos vendedores e o Total. **Fix (só no render, sem re-coleta):** `renderVendorTables()` no HTML calcula `padrao = round(totalV − somaV)` e, quando `padrao >= 100` (evita ruído de arredondamento), insere uma linha itálica "Vendedor Padrão" com esse valor antes da linha Total — Tkt/TM/Méd12M = "—" (não há contagem de tickets do resíduo). Assim vendedores + Padrão = Total (reconcilia). ⚠️ Editado em `renderVendorTables` (JS estático, FORA dos marcadores `VENDEDORES_INICIO/FIM` [dados] e `VENDGRID_INICIO/FIM` [tbodies vazios] que o build regenera) — por isso persiste entre execuções do pipeline. Ex. 04/07: L1 total 24.958 − soma 22.796 = **R$2.162** mostra; L3/L4/L5 batem (gap <R$100, ocultam). Não confundir com o cliente 8 (aquele já sai do total na fonte; o Padrão é o restante não-nomeado do total JÁ líquido).

**Card comparativo MÊS A MÊS (YoY) — automatizado nas skills (refeito 31/05/2026):** card "Faturamento mês a mês — ano corrente vs ano anterior" logo abaixo dos KPIs. Mostra TODOS os meses do ano (Jan→mês corrente), não só o mês atual. Gráfico de barras agrupadas (2 anos) + tabela mês a mês com Δ% e acumulado + botões de filtro (Grupo/L1/L3/L4/L5). Mês em curso marcado "(parcial)". **Coleta EFICIENTE (light/full, implementada 31/05/2026):** o card é mantido pelo script `atualiza_fatmensal.mjs`, que reescreve sozinho o bloco entre marcadores `FATMENSAL_INICIO/FIM` no HTML (NUNCA editar `fatMensal` à mão). Dois modos:
- `light /tmp/lojas_out.json` — reaproveita a coleta da Etapa 1 (faturamento do mês corrente que já alimenta os KPIs) e atualiza **só o último mês** de `fatMensal.atual`. **ZERO consulta nova ao ERP.** Se mês corrente ≠ nº de meses armazenados (virada de mês), sai com **exit 3** → skill roda full.
- `full /tmp/mensal_atual.json /tmp/mensal_ant.json` — reconstrói tudo a partir de `coleta_amgomes_mensal.mjs <ANO> <MES_FINAL> <DIA_FINAL>` (login 1x, loop de meses). Roda só **na segunda-feira** (`date +%u`=1, full de segurança que autocorrige histórico) ou na **virada de mês** (exit 3). Os 2 anos coletados com MESMO MES/DIA → comparação simétrica.
Lógica nas skills (Etapa 1): `DOW=1?full : light→exit3?full`. **HTML:** objeto `fatMensal {mesesLabel[12], anoAtual, anoAnterior, mesParcial, atual:{L1,L3,L4,L5}, anterior:{...}}`; `atual` e `anterior` com MESMO nº de meses (Jan→corrente). `renderYoY()` desenha gráfico+tabela+filtros a partir de `fatMensal`. Razão do ganho: ano anterior e meses fechados são imutáveis — recoletar todo dia era desperdício. Se o script falhar, bloco antigo preservado (não zera) e avisar. **Acum jan–mai 2026 vs 2025:** Grupo −12,2% · L1 +3,0% · L3 +6,8% · L4 −19,9% · L5 −27,9%. (A versão anterior do card, só do mês corrente com `maiAcum2025`/`yoyPeriodo`, foi substituída.)

**Contas a Pagar por Loja — REMOVIDA do dashboard de vendas em 19/06/2026** (pedido do usuário: "não há mais utilidade"). Era a Seção 2 (comparativo Planilha ≠ Athila/Entregue × ERP por vencimento), integrada ao pipeline em 13/06 e removida 6 dias depois. **NÃO recriar essa seção sem o usuário pedir.** O que saiu: a seção HTML + bloco `CONTASPAGAR_INICIO/FIM`, o rewriter no `build_amgomes.mjs`, e os passos de coleta (ERP faturas + parse planilha + download do xlsx via Drive MCP) no `atualizar_amgomes.sh` e nas SKILL.md. Os scripts `fetch_faturas_pagar.mjs` (ERP faturas por vencimento, Playwright headless, janela dinâmica) e `parse_planilha_pagar.py` (openpyxl da "Gestao Compras 2026.xlsx", Drive ID 1K51A55Pf5LwJlJB1PYRKldN1sJ-QZygH, exclui Status ATHILA/ENTREGUE, ALTAMIRA÷2) ficaram **dormentes** no repo (não são mais chamados) — base pronta se um dia quiser reviver. Hoje quem cobre contas a pagar é o [[app-planejamento-compras]] (OTB/Supabase).

**Cadência (confirmada 13/06/2026):** as 2 tarefas (`dashboard-amgomes-update` cron `0 12 * * 1-6` + `dashboard-amgomes-update-tarde` cron `30 18 * * 1-6`) rodam **2×/dia, segunda a sábado** (domingo não — lojas fechadas), atualizando vendas + YoY. ⚠️ O `list_scheduled_tasks` exibe o rótulo "only on Monday" para essas — é **bug cosmético** do parser de cron do MCP; o `1-6` é seg–sáb e as execuções reais (lastRunAt/nextRunAt em dias variados) comprovam que roda todo dia útil.

